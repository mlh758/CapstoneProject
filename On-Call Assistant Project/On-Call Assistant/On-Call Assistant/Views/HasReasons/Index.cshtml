@model IEnumerable<On_Call_Assistant.Models.HasReason>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.outOfOffice._date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.outOfOfficeReason.reason)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.outOfOffice._date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.outOfOfficeReason.reason)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
